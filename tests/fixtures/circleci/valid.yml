version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "building"

  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "testing"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
